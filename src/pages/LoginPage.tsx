import React from 'react';
import LoginForm from '../app/auth/containers/LoginForm';
type Props = {}
const LoginPage: React.FC<Props> = () => {
    return (
        <div>
            <LoginForm />
        </div>
    );
};

export default LoginPage;